# anon（未ログイン）が実行できる SECURITY DEFINER の関数の許容リスト（scripts/verify-migrations-from-scratch.sh の検査 3）
# ログイン前にも使う関数だけを載せる（*_definer_anon_execute.sql の時点で 2 本）。
# トリガー関数は直接呼べないので、検査にもこの一覧にも入れない。
# ここに無い関数を anon が実行できると verify が落ちる。anon から呼ぶ関数を足すときは、使う場所を添えてこの一覧に足す。
# 1 行 1 関数（public.関数名(引数の型)）。# から始まる行と空行は読み飛ばす。

# API の毎回の前処理（PostgREST の pre-request。ログイン前の API 呼び出しもこれを通る）
public.mfa_pre_request()
# ログイン前の招待ページが、招待を確かめる
public.rpc_validate_invite(text)
